import { StyleSheet } from "react-native";

export default StyleSheet.create({
  cardSectionStyle: {
    borderBottomWidth: 1,
    // borderRadius: 4,
    padding: 5,
    backgroundColor: "#fff",
    justifyContent: "flex-start",
    flexDirection: "row",
    borderColor: "#ddd",
    position: "relative"
  }
});
