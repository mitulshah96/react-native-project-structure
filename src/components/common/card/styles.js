import { StyleSheet } from "react-native";

export default StyleSheet.create({
  cardStyle: {
    borderWidth: 1,
    // borderRadius: 9,
    borderColor: "#ddd",
    borderBottomWidth: 0,
    shadowColor: "#000",
    shadowOffset: { width: 0, heigth: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 9,
    marginHorizontal: 8,
    marginTop: 10,
    elevation: 2
  }
});
